# Robinhood Chain RPC endpoint (chain ID 46630)
VITE_RH_RPC_URL=

# Optional indexer endpoint. Without it, the UI uses the on-chain SDK where available
# and a small local preview dataset for layout development.
VITE_PROTO_INDEXER_URL=

# Optional for the current direct-upload prototype. Move this behind a server route
# before production so the JWT is never shipped to browsers.
VITE_PINATA_JWT=
VITE_PINATA_GATEWAY=https://gateway.pinata.cloud/ipfs
